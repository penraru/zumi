Type: "sudo apt-get install vim"
Type: "sudo vi /etc/profile.d/JupyterStartUp.sh
In the file type:
          #!/bin/bash
          jupyter notebook
Then press esc
type :ws
Press Enter
Type: sudo chmod +x /etc/profile.d/JupyterStartUp.sh
Type: sudo reboot now
Then test it by clicking the jupyter link in front of the welcome page of the repository 
